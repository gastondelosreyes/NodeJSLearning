// function sumar(a, b) {
//     return a + b;
// }

// let sumar = (a, b) => a + b;

//let saludar = (number) => (number + 1);

//console.log(saludar(1));

let Superman = {
    name: 'Clark',
    sureName: 'Kent',
    power: 'Homosexuality',
    getName() {
        return `${this.name} ${this.sureName} got ${this.power}`;
    }
};

console.log(Superman.getName());